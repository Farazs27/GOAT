{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm db:generate && turbo run build --filter=@dentflow/web",
    "outputDirectory": "apps/web/.next",
    "installCommand": "pnpm install",
    "framework": "nextjs"
}