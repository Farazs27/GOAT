{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm db:generate && turbo run build --filter=@nexiom/web",
    "outputDirectory": "apps/web/.next",
    "installCommand": "pnpm install",
    "framework": "nextjs",
    "crons": [
        {
            "path": "/api/patient-portal/nudges/cron",
            "schedule": "0 9 * * *"
        }
    ]
}